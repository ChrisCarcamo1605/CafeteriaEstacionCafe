export interface SaveProductDTO {
  productId: number;
  name: string;
  description: string;
  price: number;
  cost: Date;
}

export interface ProductItemDTO {
  productId: number;
  name: string;
  description: string;
  price: number;
  cost: Date;
}
